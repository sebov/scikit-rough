import pandas.core.sorting
from skrough.utils.group_index import compute_dec_distribution

# def reduce(xx, xx_count_distinct, yy, yy_count_distinct, result_attrs, score_func):
#     result = list(result_attrs)
#     for i in reversed(result_attrs):
#         attrs_to_try = list(result)
#         attrs_to_try.remove(i)
#         pandas.core.sorting.get_group_index()
#
#         distribution = compute_dec_distribution(group_index, n_groups, yy, yy_count_distinct)
#         if True:
#             result = attrs_to_try
#     return result
#

